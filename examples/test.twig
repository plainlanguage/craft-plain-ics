{% set event = {
    'title': 'Test Title',
    'description': 'This is a test event.',
    'startDateTime': 'now',
    'endDateTime': 'now',
    'url': siteUrl,
} %}

{{ craft.plainIcs.render(event) }}
